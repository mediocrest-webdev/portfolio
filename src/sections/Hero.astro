<!-- Hero Section -->
<section
  id="hero"
  class="flex flex-col items-center justify-center text-center my-section-lg md:my-section-xl"
>
  <!-- Availability Notice -->
  <div class="flex items-center gap-2 badge mb-4">
    <!-- Pulsing Circle -->
    <span class="w-3 h-3 bg-green-500 rounded-full animate-pulse"></span>
    <span id="availability-text" class="text-xs text-muted">
      Available for freelance work
    </span>
  </div>

  <!-- Hero Heading -->
<h1 class="text-3xl md:text-4xl lg:text-5xl tracking-tight leading-none mb-4 md:max-w-3xl">
  Freelance Web Designer & Developer in Quezon City
</h1>

<!-- Hero Description -->
<p class="text-muted text-sm sm:text-lg max-w-[40ch] md:max-w-[60ch]">
  I’m Lance Flores — I craft minimal, user-friendly websites built on design psychology and strategy, so your business gets real results, not just a pretty site
</p>

</section>
<!-- Hero Section End -->

<!-- Inline Script to Set Current Month -->
<script>
  const availabilityEl = document.getElementById('availability-text');
  const now = new Date();
  const monthNames = [
    "January", "February", "March", "April", "May", "June",
    "July", "August", "September", "October", "November", "December"
  ];
  availabilityEl.textContent = `Available for freelance work, ${monthNames[now.getMonth()]} ${now.getFullYear()}`;
</script>
