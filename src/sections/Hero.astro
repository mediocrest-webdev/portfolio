<!-- Hero Section -->
<section
  id="hero"
  class="flex flex-col items-center justify-center text-center my-section-lg md:my-section-xl"
>
  <!-- Availability Notice -->
  <div class="flex items-center gap-2 badge mb-4">
    <!-- Pulsing Circle -->
    <span class="w-3 h-3 bg-green-500 rounded-full animate-pulse"></span>
    <span id="availability-text" class="text-xs text-muted">
      Available for freelance work
    </span>
  </div>

  <!-- Hero Heading -->
  <h1 class="text-3xl md:text-4xl lg:text-5xl tracking-tight leading-none mb-4 md:max-w-4xl">
    Freelance Web Designer in Quezon City for Growing Businesses
  </h1>

  <!-- Hero Description -->
  <p class="text-muted text-sm sm:text-lg max-w-[35ch] md:max-w-[45ch]">
    I'm Lance Flores — I build custom, SEO-friendly websites for businesses in Quezon City, Manila, and beyond.
  </p>
</section>
<!-- Hero Section End -->

<!-- Inline Script to Set Current Month -->
<script>
  const availabilityEl = document.getElementById('availability-text');
  const now = new Date();
  const monthNames = [
    "January", "February", "March", "April", "May", "June",
    "July", "August", "September", "October", "November", "December"
  ];
  availabilityEl.textContent = `Available for freelance work, ${monthNames[now.getMonth()]} ${now.getFullYear()}`;
</script>
